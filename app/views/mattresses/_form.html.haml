%h1 New Shop
= form_with(model: mattress, url: mattress) do |form|
  - if mattress.errors.any?
    %div{ style: 'color: red' }
      %h2
        = pluralize(mattress.errors.count, 'error')
        prohibited this mattress from being saved:
      %ul
        - mattress.errors.each do |error|
          %li= error.full_message
  %div
    = form.label :shop_id, style: 'display: block'
    = form.collection_select :shop_id, shops, :shop_id, :shop
  %div
    = form.submit
